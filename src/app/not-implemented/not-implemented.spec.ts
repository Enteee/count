import { NotImplemented } from './not-implemented';

describe('NotImplemented', () => {
  let mockRenderer, mockElementRef, mockModalController, MockAppStateRepositoryService;

  it('should create an instance', () => {
    expect(new NotImplemented(
      {} as any,
      {} as any,
      {} as any,
      {} as any,
    )).toBeTruthy();
  });
});
